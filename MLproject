name: my-ml-project

conda_env: conda.yaml

entry_points:
  download_data:
    command: "python download_data.py"
  train_model:
    command: "python train_model.py"

dependencies:
  - python=3.8
  - scikit-learn=0.24.2
  - pandas=1.2.4
  - requests=2.26.0
  - pip
